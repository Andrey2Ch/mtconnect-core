# Локальная разработка MTConnect
MONGODB_URI=mongodb://admin:password@localhost:27017/mtconnect?authSource=admin
NODE_ENV=development
API_KEY=edge-gateway-api-key

# Railway продакшн переменные (для справки)
# MONGODB_URI будет взят из Railway в продакшн 